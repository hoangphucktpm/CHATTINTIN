import {StyleSheet} from 'react-native'
const styles = StyleSheet.create({
    container:{
        flex:1,
    },
    footter:{
        position: 'absolute',
        bottom:0,
        height:100,
        width:"100%"
    }
})

export default styles